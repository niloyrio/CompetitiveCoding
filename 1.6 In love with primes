for x in range(int(input())):
    n=int(input())
    l=[]
    for j in range(2,n):
        for k in range(2,j):
            if (j % k) == 0:
                break
            else:
                l.append(j)
    for a in range(len(l)-1):
        for b in range(a,len(l)-1):
            if l[a]+l[b]==n:
                print("Deepa")